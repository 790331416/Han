# Release Gate

## 触发条件

- 任务涉及部署、发版、配置切换、服务启停、数据库变更、95 发布。

## 必做动作

- 先确认目标环境、版本、目录、启动方式、覆盖策略、回滚方式。
- 先确认必要测试、审批、观测和停机窗口。
- 95 发布必须遵守 `/opt/han/repo/Han` 与 `/opt/han/deploy/{small,medium,full}` 固定链路。
- 发布失败或指标异常时，优先回滚，再定位。

## 交付要求

- 列出门禁是否满足。
- 列出发布后的验证项。
- 列出回滚触发条件。
