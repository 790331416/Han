spring:
  profiles:
    active: @profiles.active@
  application:
    name: xuman-file
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml

# 开发环境配置
server:
  port: 9207
