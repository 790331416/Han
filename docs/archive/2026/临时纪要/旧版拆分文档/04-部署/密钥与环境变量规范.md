# 密钥与环境变量规范

## 原则

- secrets 不进 Git
- 只通过受控环境变量文件或 secrets 管理位置注入
- 同一 tier 的服务共享同一套必要密钥来源

## 必查变量

- `HAN_DEPLOY_TIER`
- `HAN_INNER_AUTH_SECRET`
- AI provider 相关密钥
- 对象存储与中间件连接信息

## 95 约束

- 95 上的正式环境变量必须持久化
- 不能只靠临时 shell `export`
