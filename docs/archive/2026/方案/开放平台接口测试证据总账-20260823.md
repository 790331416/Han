# 开放平台接口测试证据总账（2026-08-23）

> 状态：**总账完成，18 个行为测试待补**。数据源版本：`5e5fe66`。
> 本账以代码仓库中的真实 Controller/Handler、路由与测试类为唯一来源，不引用任何外部「覆盖率达到多少」的结论。
> 数据源版本：`three-class-manage-han` 工作区，`han-open` 模块 `com.han.open` 包、`han-ui` 前端、`han-auth`/`han-system` 交叉模块测试。
> 证据层级口径：
> - **自动化测试**：仓库内可独立复跑的 JUnit / Playwright 用例（含 Mock 单测与 mock-API 的 e2e）。
> - **临时数据库**：需要真实后端 + 临时数据才能跑的通 e2e（本账未发现已执行的此类记录）。
> - **生产只读 / 生产主链路**：生产环境实测证据——**均无**，一律记为「生产未执行/无证据」，不补表。
> - **无证据**：连注解/反射规范级用例都没有覆盖到。

## 一、后端 Handler 总账（han-open，55 个 handler 方法）

说明：
- 「认证方式」列取实际注解：`@AdminAuth`（类级登录+管理员校验）、`@PreAuthorize`（权限点）、`@PermissionExempt`（匿名，业务内自校验）、`@InnerAuth`（内部服务互信）、`@IgnoreTenant`（跳过租户行级过滤）、`@RateLimiter`（限流）。
- 「正例证据 / 负例边界」列格式为 `测试类::方法`，只列真实存在的测试方法；多个以 `；` 分隔。
- 「证据层级」按上述口径逐行判定；「未覆盖原因」只填确有缺口的情形。

### 1. OAuth2Controller（`/oauth2`、`/open/oauth2`，8 个 handler）

| 模块 | Controller | 方法 | 实际路由 | 认证方式 | 正例证据 | 负例/边界覆盖 | 证据层级 | 未覆盖原因 |
|---|---|---|---|---|---|---|---|---|
| han-open | OAuth2Controller | authorize | GET /oauth2/authorize 或 /open/oauth2/authorize | @PermissionExempt；代码内 `SecurityContextHolder.isLogin()` | 无行为级用例 | 无（未登录 401「请先登录」未测） | 仅注解规范（OpenPlatformControllerStandardsTest::publicAndInternalBoundariesAreExplicit 断言 @PermissionExempt） | 授权码签发链路无单测 |
| han-open | OAuth2Controller | authorizeConfirm | POST /oauth2/authorize/confirm 或 /open/oauth2/authorize/confirm | @PermissionExempt；代码内登录态+approved 校验 | 无行为级用例 | 无（拒绝授权/未登录分支未测） | 仅注解规范 | 同上 |
| han-open | OAuth2Controller | token | POST /oauth2/token 或 /open/oauth2/token（form-urlencoded） | @PermissionExempt；client_id+client_secret+grant 校验 | OAuth2ServiceImplTest::persistsClientCredentialsTokensInRedis；OAuth2ServiceImplTest::issuesEnvironmentCredentialAndChecksItsVendorGrant | OAuth2ServiceImplTest::rejectsScopeOutsideTheApplicationGrantBeforeIssuingAToken；OAuth2ServiceImplTest::rejectsNewTokenWhenVendorIsStopped | 自动化测试（服务层） | Controller 层参数绑定未直测 |
| han-open | OAuth2Controller | tokenJson | POST /oauth2/token 或 /open/oauth2/token（application/json） | @PermissionExempt；同上 | 同 token（`service.token(dto)` 同一入口） | 同 token | 自动化测试（服务层） | 同上 |
| han-open | OAuth2Controller | revoke | POST /oauth2/revoke 或 /open/oauth2/revoke | @PermissionExempt；client_id+client_secret | OAuth2TokenLifecycleTest::revokeDeletesAccessAndRefreshTokenTogether | 无（错误 client_secret 分支未测） | 自动化测试（服务层） | Controller 层凭证校验未直测 |
| han-open | OAuth2Controller | introspect | POST /oauth2/introspect 或 /open/oauth2/introspect | @PermissionExempt；client_id+client_secret | OAuth2TokenLifecycleTest::introspectMarksExpiredAccessTokenInactiveAndDeletesIt | 无 | 自动化测试（服务层） | Controller 层凭证校验未直测 |
| han-open | OAuth2Controller | userInfo | GET /oauth2/userinfo 或 /open/oauth2/userinfo | @PermissionExempt；Bearer access_token | 无行为级用例 | 无 | 仅注解规范 | `getUserInfo` 服务方法无单测 |
| han-open | OAuth2Controller | discovery | GET /oauth2/.well-known/openid-configuration 或 /open/oauth2/.well-known/openid-configuration | @PermissionExempt | OAuth2ControllerTest::discoveryReturnsConcreteMetadata | 无 | 自动化测试（Controller 直测） | — |

### 2. OpenApiAdminController（`/open/admin`，1 个 handler）

| 模块 | Controller | 方法 | 实际路由 | 认证方式 | 正例证据 | 负例/边界覆盖 | 证据层级 | 未覆盖原因 |
|---|---|---|---|---|---|---|---|---|
| han-open | OpenApiAdminController | refreshResourceCache | POST /open/admin/resource/cache/refresh | @IgnoreTenant + @PreAuthorize(open:admin:config) | ResourcePathMappingServiceImplTest::refreshRemovesDisabledAndDeletedResourcesFromSnapshot（服务间接） | ResourcePathMappingServiceImplTest::failedRefreshKeepsLastCompleteSnapshot | 自动化测试（服务层间接）；注解规范（OpenPlatformControllerStandardsTest::allOpenPlatformBusinessWritesHavePostPermissionAndReplayProtection） | 权限拒绝分支、Controller 直测缺失 |

### 3. OpenApiResourceController（`/open/app/api-resources`、`/open/api-resource`，11 个 handler）

| 模块 | Controller | 方法 | 实际路由 | 认证方式 | 正例证据 | 负例/边界覆盖 | 证据层级 | 未覆盖原因 |
|---|---|---|---|---|---|---|---|---|
| han-open | OpenApiResourceController | list | GET /open/app/api-resources 或 /open/api-resource/list | @AdminAuth + @IgnoreTenant + @PreAuthorize(open:app:query\|open:api-resource:list\|open:api-resource:query) | OpenApiResourceControllerTest::adminListCanIncludeDisabledResources | 无 | 自动化测试（Controller 直测） | 权限拒绝分支未测 |
| han-open | OpenApiResourceController | add | POST /open/api-resource | @AdminAuth + @IgnoreTenant + @PreAuthorize(open:api-resource:add) + @RepeatSubmit + @OperLog | OpenApiResourceControllerTest::successfulMutationsRefreshResourceMapping | OpenApiResourceControllerTest::failedMutationDoesNotRefreshResourceMapping | 自动化测试（Controller 直测） | 校验（名称/编码/path 前缀）负例未直测 |
| han-open | OpenApiResourceController | edit | POST /open/api-resource/edit | @AdminAuth + @IgnoreTenant + @PreAuthorize(open:api-resource:edit) + @RepeatSubmit + @OperLog | OpenApiResourceControllerTest::successfulMutationsRefreshResourceMapping | 同上 failed 分支 | 自动化测试（Controller 直测） | — |
| han-open | OpenApiResourceController | remove | POST /open/api-resource/remove/{id} | @AdminAuth + @IgnoreTenant + @PreAuthorize(open:api-resource:remove) + @RepeatSubmit + @OperLog | OpenApiResourceControllerTest::successfulMutationsRefreshResourceMapping | 同上 failed 分支 | 自动化测试（Controller 直测） | — |
| han-open | OpenApiResourceController | changeStatus | POST /open/api-resource/changeStatus | @AdminAuth + @IgnoreTenant + @PreAuthorize(open:api-resource:edit) + @RepeatSubmit + @OperLog | OpenApiResourceControllerTest::successfulMutationsRefreshResourceMapping | 同上 failed 分支 | 自动化测试（Controller 直测） | 状态参数为空负例未直测 |
| han-open | OpenApiResourceController | offline | POST /open/api-resource/offline/{id} | @AdminAuth + @IgnoreTenant + @PreAuthorize(open:api-resource:edit) + @RepeatSubmit + @OperLog | 无行为级用例 | 无 | 仅注解规范（OpenPlatformControllerStandardsTest 资源写端点枚举） | 下线行为无单测 |
| han-open | OpenApiResourceController | getDetail | GET /open/api-resource/{id} | @AdminAuth + @IgnoreTenant + @PreAuthorize(open:api-resource:query) | 无行为级用例 | 无 | 仅注解规范（protectedReads 枚举） | `getDetail` 服务方法无单测 |
| han-open | OpenApiResourceController | createDraftVersion | POST /open/api-resource/{resourceId}/versions | @AdminAuth + @IgnoreTenant + @PreAuthorize(open:api-resource:edit) + @RepeatSubmit + @OperLog | OpenApiResourceServiceImplTest::createDraftValidatesOpenApiPathMethodAndExamples | OpenApiResourceServiceImplTest::createDraftRejectsSchemaWithoutResourceOperation | 自动化测试（服务层） | Controller 直测缺失 |
| han-open | OpenApiResourceController | updateDraftVersion | POST /open/api-resource/versions/edit | @AdminAuth + @IgnoreTenant + @PreAuthorize(open:api-resource:edit) + @RepeatSubmit + @OperLog | OpenApiResourceServiceImplTest::publishedVersionCannotBeEdited | 同左（已发布不可编辑） | 自动化测试（服务层） | Controller 直测缺失 |
| han-open | OpenApiResourceController | publishVersion | POST /open/api-resource/versions/{versionId}/publish | @AdminAuth + @IgnoreTenant + @PreAuthorize(open:api-resource:edit) + @RepeatSubmit + @OperLog | OpenApiResourceServiceImplTest::publishRetiresPreviousVersionAndMarksResourcePublished | 无 | 自动化测试（服务层） | Controller 直测缺失 |
| han-open | OpenApiResourceController | deprecateVersion | POST /open/api-resource/versions/{versionId}/deprecate | @AdminAuth + @IgnoreTenant + @PreAuthorize(open:api-resource:edit) + @RepeatSubmit + @OperLog | 无行为级用例 | 无 | 仅注解规范 | 废弃版本行为无单测 |

### 4. OpenApiTestRunController（`/open/debug/run`，2 个 handler）

| 模块 | Controller | 方法 | 实际路由 | 认证方式 | 正例证据 | 负例/边界覆盖 | 证据层级 | 未覆盖原因 |
|---|---|---|---|---|---|---|---|---|
| han-open | OpenApiTestRunController | add | POST /open/debug/run/add | @AdminAuth + @PreAuthorize(open:grant:apply) + @RepeatSubmit | OpenApiTestRunControllerTest::controllerExposesScopedRoutesAndDelegates；OpenApiTestRunServiceImplTest::addRebuildsMethodAndPathAndStoresOnlyRedactedSummary | OpenApiTestRunServiceImplTest::viewerCannotSubmitRun；::clientCannotMarkServerErrorAsSuccess；::disabledUnpublishedOrNotTestableResourceIsRejected；::missingOrExpiredGrantIsRejected；::stoppedVendorCannotSubmitOnlineDebugRun；::crossVendorApplicationIsRejected | 自动化测试（Controller+服务层） | — |
| han-open | OpenApiTestRunController | list | GET /open/debug/run/list | @AdminAuth + @PreAuthorize(open:grant:query) | OpenApiTestRunControllerTest::controllerExposesScopedRoutesAndDelegates；OpenApiTestRunServiceImplTest::listUsesCurrentTenantAndVendorApplicationScope | 无 | 自动化测试（Controller+服务层） | — |

### 5. OpenAppAuthorizationController（`/open/authorization`，8 个 handler）

| 模块 | Controller | 方法 | 实际路由 | 认证方式 | 正例证据 | 负例/边界覆盖 | 证据层级 | 未覆盖原因 |
|---|---|---|---|---|---|---|---|---|
| han-open | OpenAppAuthorizationController | requests | GET /open/authorization/requests 或 /open/authorization/request/list | @AdminAuth + @PreAuthorize(open:grant:query) | OpenAdminQueryServiceTest::authorizationRequestPageCopiesRequestId；OpenAppAuthorizationServiceImplTest::viewerListQueriesAreScopedToOwnVendorApps | 无 | 自动化测试（服务层） | Controller 直测缺失 |
| han-open | OpenAppAuthorizationController | credentials | GET /open/authorization/credentials 或 /open/authorization/credential/list | @AdminAuth + @PreAuthorize(open:credential:query) | OpenAdminQueryServiceTest::credentialQueryReturnsOnlyNonSecretFields；OpenAppAuthorizationServiceImplTest::adminCanListCredentialsAcrossVendors | OpenAppAuthorizationServiceImplTest::viewerListQueriesAreScopedToOwnVendorApps（范围收窄） | 自动化测试（服务层） | Controller 直测缺失 |
| han-open | OpenAppAuthorizationController | submitGrantApply | POST /open/authorization/apply | @AdminAuth + @PreAuthorize(open:grant:apply) + @RepeatSubmit + @OperLog | OpenAppAuthorizationEnvironmentTest::submitGrantApplyPersistsEnvironment | OpenAppAuthorizationEnvironmentTest::submitGrantApplyRejectsPendingGrantAsDuplicate；::submitGrantApplyRejectsClientScopeOutsideResourceCatalog；::submitGrantApplyRejectsInvalidEnvironment；::submitGrantApplyRejectsUserOutsideVendor；::submitGrantApplyRejectsUnpublishedResource；OpenAppAuthorizationServiceImplTest::viewerCannotSubmitGrantApply；::submitGrantApplyRejectsAppFromAnotherTenant | 自动化测试（服务层） | Controller 直测缺失 |
| han-open | OpenAppAuthorizationController | reviewGrantApply | POST/PUT /open/authorization/review/{requestId} | @AdminAuth + @PreAuthorize(open:grant:review) + @RepeatSubmit + @OperLog | OpenAppAuthorizationEnvironmentTest::reviewApprovalPersistsGrantWithRequestEnvironment | OpenAppAuthorizationEnvironmentTest::reviewGrantApplyRejectsConcurrentReviewClaim；::reviewGrantApplyRejectsUnsupportedStatusBeforeMutation；OpenAppAuthorizationServiceImplTest::nonAdminCannotReviewGrantApply；::adminCannotReviewRequestFromAnotherTenant；::adminCanReviewRequestFromAnotherVendor | 自动化测试（服务层） | Controller 直测缺失 |
| han-open | OpenAppAuthorizationController | listAppGrants | GET /open/authorization/app/{appId} | @AdminAuth + @PreAuthorize(open:grant:query) | OpenAppAuthorizationServiceImplTest::listAppGrantsScopesQueryToCurrentTenant | OpenAppAuthorizationServiceImplTest::listAppGrantsThrowsWhenTenantContextMissing；::viewerListsOnlyAppsFromOwnVendor | 自动化测试（服务层） | Controller 直测缺失 |
| han-open | OpenAppAuthorizationController | revokeGrant | POST/PUT /open/authorization/revoke/{grantId} | @AdminAuth + @PreAuthorize(open:grant:revoke) + @RepeatSubmit + @OperLog | OpenAppAuthorizationServiceImplTest::revokeGrantPersistsReviewerAndUpdaterFromRealUser | OpenAppAuthorizationServiceImplTest::revokeGrantThrowsWhenUserContextMissing；::viewerCannotRevokeGrant | 自动化测试（服务层） | Controller 直测缺失 |
| han-open | OpenAppAuthorizationController | generateCredential | POST /open/authorization/credential/generate | @AdminAuth + @PreAuthorize(open:credential:manage) + @RepeatSubmit + @OperLog(saveParams=false,saveResult=false) | OpenAppAuthorizationServiceImplTest::generateCredentialPersistsCreatorAppIdAndEnvironmentFromRealUser | OpenAppAuthorizationServiceImplTest::generateCredentialThrowsWhenUserContextMissing；::viewerCannotGenerateCredential；::generateCredentialRejectsEnvironmentBeforeLifecycleGate | 自动化测试（服务层） | Controller 直测缺失 |
| han-open | OpenAppAuthorizationController | rotateCredential | POST/PUT /open/authorization/credential/rotate/{credentialId} | @AdminAuth + @PreAuthorize(open:credential:manage) + @RepeatSubmit + @OperLog(saveParams=false,saveResult=false) | OpenAppAuthorizationServiceImplTest::rotateCredentialRequiresOwnedAppAndPersistsReplacement | OpenAppAuthorizationServiceImplTest::rotateCredentialRejectsCredentialFromAnotherTenant | 自动化测试（服务层） | Controller 直测缺失 |

### 6. OpenAppController（`/open/app`，8 个 handler）

| 模块 | Controller | 方法 | 实际路由 | 认证方式 | 正例证据 | 负例/边界覆盖 | 证据层级 | 未覆盖原因 |
|---|---|---|---|---|---|---|---|---|
| han-open | OpenAppController | list | GET /open/app/list | @AdminAuth + @PreAuthorize(open:app:list) | 无（`selectPage` 无单测）；e2e 存在未执行 | 无 | 仅注解规范 | 列表分页无单测；open-app.spec 未执行 |
| han-open | OpenAppController | getInfo | GET /open/app/{appId} | @AdminAuth + @PreAuthorize(open:app:query) | OpenAppServiceImplTest::crossVendorAppIsNotVisibleToAnotherVendorUser（负例） | 同左 | 自动化测试（服务层，仅负例） | 正向详情查询无单测 |
| han-open | OpenAppController | add | POST /open/app | @AdminAuth + @PreAuthorize(open:app:add) + @RepeatSubmit + @OperLog(saveParams=false,saveResult=false) | OpenAppServiceImplTest::vendorUserCreatesDraftWithoutProductionSecret；::administratorKeepsLegacyCredentialCreationUsable | OpenAppServiceImplTest::vendorDraftCannotValidateClient（生命周期门禁） | 自动化测试（服务层） | Controller 直测缺失 |
| han-open | OpenAppController | edit | POST /open/app/edit | @AdminAuth + @PreAuthorize(open:app:edit) + @RepeatSubmit + @OperLog | 无（`update` 无单测）；e2e 存在未执行 | 无 | 仅注解规范 | 更新逻辑无单测；open-app.spec 未执行 |
| han-open | OpenAppController | remove | POST /open/app/remove/{appId} | @AdminAuth + @PreAuthorize(open:app:remove) + @RepeatSubmit + @OperLog | 无（`deleteById` 无单测）；e2e 存在未执行 | 无 | 仅注解规范 | 删除逻辑无单测；open-app.spec 未执行 |
| han-open | OpenAppController | resetSecret | POST /open/app/resetSecret/{appId} | @AdminAuth + @PreAuthorize(open:app:resetSecret) + @RepeatSubmit + @OperLog(saveParams=false,saveResult=false) | 无（`resetAppSecret` 无单测）；e2e 存在未执行 | 无 | 仅注解规范（OpenPlatformControllerStandardsTest::adminWriteEndpointsUseRepeatSubmitAndSafeOperationLogs 校验 saveParams/saveResult） | 重置密钥无单测；open-app.spec 未执行 |
| han-open | OpenAppController | changeStatus | POST /open/app/changeStatus | @AdminAuth + @PreAuthorize(open:app:edit) + @RepeatSubmit + @OperLog | 无（`updateStatus` 无单测）；e2e 存在未执行 | 无 | 仅注解规范 | 状态变更无单测；open-app.spec 未执行 |
| han-open | OpenAppController | changeLifecycleStatus | POST /open/app/changeLifecycleStatus | @AdminAuth + @PreAuthorize(open:app:edit) + @RepeatSubmit + @OperLog | OpenAppServiceImplTest::vendorUserCanEnterTestingAfterSandboxApproval；::administratorCanApproveLegalLifecycleTransitionOnly | OpenAppServiceImplTest::vendorUserCanSubmitDraftButCannotJumpToProduction；::administratorCanApproveLegalLifecycleTransitionOnly（非法跳转） | 自动化测试（服务层） | Controller 直测缺失 |

### 7. OpenDirectoryController（`/open/api/v1/directory`，3 个 handler）

| 模块 | Controller | 方法 | 实际路由 | 认证方式 | 正例证据 | 负例/边界覆盖 | 证据层级 | 未覆盖原因 |
|---|---|---|---|---|---|---|---|---|
| han-open | OpenDirectoryController | teachers | GET /open/api/v1/directory/teachers | Bearer access_token（scope=edu.teacher.read，resource=directory.teachers.read）+ @RateLimiter(IP) | OpenDirectoryControllerTest::teachersPassesTeacherScopeAndDelegatesToSystemService | OpenDirectoryControllerTest::missingAuthorizationHeaderThrowsMissingBearerToken；::nonBearerAuthorizationThrowsMissingBearerToken；::schoolOutsideTokenGrantThrowsNotAuthorized；e2e open-platform-portal.spec 验证 Bearer 透传 | 自动化测试（Controller 直测 + e2e 已执行） | 限流行为无测试 |
| han-open | OpenDirectoryController | students | GET /open/api/v1/directory/students | Bearer（scope=edu.student.read，resource=directory.students.read）+ @RateLimiter(IP) | OpenDirectoryControllerTest::studentsPassesStudentScopeAndDelegatesToSystemService | OpenDirectoryControllerTest::basicAuthorizationThrowsMissingBearerToken | 自动化测试（Controller 直测） | 限流行为无测试 |
| han-open | OpenDirectoryController | devices | GET /open/api/v1/directory/devices | Bearer（scope=edu.device.read，resource=directory.devices.read）+ @RateLimiter(IP) | OpenDirectoryControllerTest::devicesPassesDeviceScopeAndDelegatesToSystemService | 无（缺 scope 分支在 context() 统一处理，未见 devices 负例） | 自动化测试（Controller 直测） | — |

### 8. OpenVendorController（`/open/vendor`，8 个 handler）

| 模块 | Controller | 方法 | 实际路由 | 认证方式 | 正例证据 | 负例/边界覆盖 | 证据层级 | 未覆盖原因 |
|---|---|---|---|---|---|---|---|---|
| han-open | OpenVendorController | list | GET /open/vendor/list | @AdminAuth + @PreAuthorize(open:vendor:list) | 无（`listPage` 无单测） | 无 | 仅注解规范 | 厂商分页无单测 |
| han-open | OpenVendorController | applications | GET /open/vendor/applications | @AdminAuth + @PreAuthorize(open:vendor:list) | OpenAdminQueryServiceTest::vendorApplicationPageExposesApplicationIdAndPagination | 无 | 自动化测试（服务层） | Controller 直测缺失 |
| han-open | OpenVendorController | submitApplication | POST /open/vendor/application | @AdminAuth + @PreAuthorize(open:vendor:apply) + @RepeatSubmit + @OperLog(saveParams=false) | OpenVendorServiceImplTest::submitApplicationPersistsRealTenantAndUserIdsInsteadOfHardcodedOne | OpenVendorServiceImplTest::submitApplicationThrowsWhenUserContextMissing；::submitApplicationThrowsWhenTenantContextMissing；::submitApplicationRejectsDuplicateQualificationInSameTenant | 自动化测试（服务层） | Controller 直测缺失 |
| han-open | OpenVendorController | reviewApplication | POST/PUT /open/vendor/application/review/{id} | @AdminAuth + @PreAuthorize(open:vendor:review) + @RepeatSubmit + @OperLog | OpenVendorPortalServiceTest::approvesPublicApplicationAndActivatesAccount | OpenVendorServiceImplTest::reviewApplicationRejectsIllegalReviewStatus；OpenVendorPortalServiceTest::rejectsPublicApplicationWithoutActivatingAccount | 自动化测试（服务层） | Controller 直测缺失 |
| han-open | OpenVendorController | getDetail | GET /open/vendor/{id} | @AdminAuth + @PreAuthorize(open:vendor:query) | 无（`getDetail` 无单测） | 无 | 仅注解规范 | 厂商详情无单测 |
| han-open | OpenVendorController | listMyVendors | GET /open/vendor/my | @AdminAuth + @PreAuthorize(open:vendor:my) | 无（`listByUserId` 无单测） | 无 | 仅注解规范（OpenVendorControllerStandardsTest::vendorControllerUsesAdminAuthPermissionsAndSafeApplicationLogging 校验权限值） | 当前用户厂商列表无单测 |
| han-open | OpenVendorController | bindUser | POST /open/vendor/{vendorId}/bind-user | @AdminAuth + @PreAuthorize(open:vendor:manage) + @RepeatSubmit + @OperLog | 无正向用例 | OpenVendorServiceImplTest::bindUserRejectsInvalidRoleBeforeWriting；::bindUserRequiresOwnerOrAdministrator | 自动化测试（服务层，仅负例） | 正向绑定无单测；Controller 直测缺失 |
| han-open | OpenVendorController | updateStatus | POST/PUT /open/vendor/{vendorId}/status | @AdminAuth + @PreAuthorize(open:vendor:manage) + @RepeatSubmit + @OperLog | 无（`updateStatus` 无单测） | 无 | 仅注解规范 | 厂商状态变更无单测 |

### 9. SsoController（`/sso`、`/open/sso`，4 个 handler）

| 模块 | Controller | 方法 | 实际路由 | 认证方式 | 正例证据 | 负例/边界覆盖 | 证据层级 | 未覆盖原因 |
|---|---|---|---|---|---|---|---|---|
| han-open | SsoController | ssoLogin | GET /sso/login 或 /open/sso/login | @PermissionExempt；代码内登录态 + validateRedirectUri | 无行为级用例 | 无（未登录/redirect_uri 非法未测） | 仅注解规范 | 登录换 ticket 链路无单测 |
| han-open | SsoController | ssoLogout | GET /sso/logout 或 /open/sso/logout | @PermissionExempt；可选 Bearer 清登录态 | 无行为级用例 | 无 | 仅注解规范 | 登出清 token 逻辑无单测 |
| han-open | SsoController | validateTicket | POST /sso/validate 或 /open/sso/validate | @PermissionExempt；client_id+client_secret+ticket | SsoControllerTest::validateTicketConsumesValidTicket | SsoControllerTest::validateTicketRejectsInvalidClient；::validateTicketRejectsExpiredTicketAsUnauthorized；::validateTicketRejectsClientMismatchAsForbidden | 自动化测试（Controller 直测） | — |
| han-open | SsoController | checkLogin | GET /sso/check 或 /open/sso/check | @PermissionExempt | 无行为级用例 | 无 | 仅注解规范 | 极简端点，无单测 |

### 10. inner/IOpenVendorController（`/inner/open/vendor`，2 个 handler，内部 Feign/HttpExchange 服务端）

| 模块 | Controller | 方法 | 实际路由 | 认证方式 | 正例证据 | 负例/边界覆盖 | 证据层级 | 未覆盖原因 |
|---|---|---|---|---|---|---|---|---|
| han-open | inner/IOpenVendorController | createPortalApplication | POST /inner/open/vendor/application | @InnerAuth（无 @PreAuthorize） | OpenVendorPortalServiceTest::createsPortalApplicationWithRandom32CharacterNumberAndOwner；han-auth VendorRegistrationServiceTest::decryptsRegistrationPasswordBeforeCreatingDisabledAccount（客户端编排） | OpenVendorPortalServiceTest::rejectsDuplicateEnterpriseBeforeWriting；::retriesSameOwnerPublicApplicationIdempotently；::retriesAfterConcurrentVendorInsertConflict；han-auth VendorRegistrationServiceTest::compensatesDisabledAccountWhenOpenApplicationFails；::doesNotCompensateWhenSystemResponseIsUnknown；::doesNotCompensateWhenOpenResponseIsUnknownAndRetryCanRecover | 自动化测试（服务层 + 跨模块客户端） | Controller 直测（@InnerAuth 头部）缺失 |
| han-open | inner/IOpenVendorController | queryPortalApplication | GET /inner/open/vendor/application/status | @InnerAuth（无 @PreAuthorize） | OpenVendorPortalServiceTest::publicStatusLookupScopesVendorToPlatformTenantAndNotDeletedRows | OpenVendorPortalServiceTest::anonymousStatusDoesNotExposeLegacyApplication | 自动化测试（服务层） | Controller 直测缺失 |

### 11. 交叉模块内部接口（@Service 暴露为接口的逻辑）

| 模块 | 接口/服务 | 相关内部路由 | 说明 | 正例证据 | 负例/边界覆盖 | 证据层级 |
|---|---|---|---|---|---|---|
| han-api-open | OpenServiceClient | /inner/open/vendor/application、/inner/open/vendor/application/status | han-auth 调 han-open 的厂商门户内部客户端（@HttpExchange） | 见 IOpenVendorController 两行 | han-auth VendorRegistrationServiceTest 系列 | 自动化测试 |
| han-api-system | SystemServiceClient | /inner/system/vendor/portal/account（create/activate/compensate） | han-open/han-auth 调 han-system 的门户账号内部客户端（@HttpExchange） | han-system OpenVendorAccountServiceTest::createsDisabledAccountWithOnlyOpenVendorRole | OpenVendorAccountServiceTest::rejectsWeakPasswordBeforeDatabaseAccess；::rejectsUsernameDuplicatedAcrossTenants；::retriesSameDisabledOpenVendorAccountIdempotently；::rejectsRetryWhenPasswordOrPhoneDiffers；::activatesOnlyDisabledOpenVendorAccount；::activatingAlreadyActiveOpenVendorAccountIsIdempotent；::compensationRenamesAndClearsPhoneBeforeLogicalDelete | 自动化测试（han-system 模块） |
| han-api-system | SystemServiceClient | /inner/system/external/directory/people、/inner/system/external/directory/devices | han-open OpenDirectoryController 读取教育目录的内部接口 | OpenDirectoryControllerTest 系列（消费侧） | 同左 | 自动化测试（消费侧） |

> 说明：`SystemServiceClient` / `OpenServiceClient` 均为 `@HttpExchange` 声明式客户端（仓库内**没有** `@FeignClient`），对应服务端分别在 han-system 与 han-open 的 inner 控制器。han-system 侧 handler 不属于本账「han-open 控制器」计数，仅作为交叉证据列出。

## 二、前端页面 / 路由 / API 映射（han-ui）

### 2.1 静态路由（`han-ui/src/router/index.ts`）

| 前端路由 | 组件 | 性质 | 白名单 | 后端 API 关联 | e2e 证据 |
|---|---|---|---|---|---|
| /open/vendor-apply | views/open/vendor-apply/index.vue | 公开厂商入驻申请页 | whiteList 含 `/open/vendor-apply` | /auth/vendor/publicKey、/auth/vendor/register、/auth/vendor/application/status（走 han-auth，非 han-open） | open-vendor-apply.spec.ts（5 用例，mock；**未在执行记录中**） |

### 2.2 动态菜单路由（由后端 /system/menu/routers 下发，component 指向 views/open/*）

| 前端路由 | 组件 | 页面用途 | 主要后端 API（han-open） | e2e 证据 |
|---|---|---|---|---|
| /open/api-resource | views/open/api-resource/index.vue | 接口目录管理（admin） | /open/api-resource/list、/open/api-resource、/open/api-resource/edit、/open/api-resource/remove/{id}、/open/api-resource/changeStatus、/open/api-resource/offline/{id}、版本四接口 | open-platform-admin.spec.ts（3 用例，mock；**已执行且通过**） |
| /open/app | views/open/app/index.vue | 应用管理（admin） | /open/app/list、/open/app/{id}、/open/app、/open/app/edit、/open/app/remove/{appId}、/open/app/resetSecret/{appId}、/open/app/changeStatus、/open/app/changeLifecycleStatus、/open/app/api-resources | open-app.spec.ts（1 用例，真实后端；**未在执行记录中**） |
| /open/authorization | views/open/authorization/index.vue | 授权管理（admin） | /open/authorization/request/list、/review/{id}、/app/{appId}、/revoke/{id}、/credential/list、/credential/generate、/credential/rotate/{id} | 无 e2e |
| /open/vendor | views/open/vendor/index.vue | 厂商管理（admin） | /open/vendor/list、/{id}、/applications、/application、/application/review/{id}、/{id}/status、/{vendorId}/bind-user | 无 e2e |
| /open/portal | views/open/portal/index.vue | 厂商门户（厂商自助） | /open/vendor/my、/open/app/list、/open/api-resource/list、/open/authorization/apply、/open/debug/run/add、/open/debug/run/list、/open/authorization/*、/open/oauth2/token（前端 fetch） | open-platform-portal.spec.ts（3 用例，mock；**已执行且通过**） |

### 2.3 前端 API 文件 → 后端路由（`han-ui/src/api/open/*`）

| 文件 | 后端路由（节选） |
|---|---|
| app.ts | /open/app/list、/open/app/{id}、/open/app、/open/app/edit、/open/app/remove/{id}、/open/app/resetSecret/{appId}、/open/app/changeStatus、/open/app/changeLifecycleStatus、/open/app/api-resources |
| authorization.ts | /open/authorization/request/list、/review/{id}、/app/{appId}、/revoke/{id}、/credential/list、/credential/generate、/credential/rotate/{id} |
| resource.ts | /open/api-resource/list、/{id}、POST /open/api-resource、/edit、/remove/{id}、/changeStatus、/offline/{id}、/{resourceId}/versions、/versions/edit、/versions/{versionId}/publish、/versions/{versionId}/deprecate |
| vendor.ts | /open/vendor/list、/{id}、/applications、/application、/application/review/{id}、/{id}/status、/{vendorId}/bind-user |
| portal.ts | /auth/vendor/publicKey、/auth/vendor/register、/auth/vendor/application/status、/open/authorization/apply、/open/vendor/my、/open/debug/run/add、/open/debug/run/list |

## 三、Playwright e2e 证据（`han-ui/tests/e2e/specs/`）

| 文件 | 用例数 | 类型 | 是否在执行记录中 | 状态 | 覆盖路由 |
|---|---|---|---|---|---|
| open-platform-admin.spec.ts | 3 | mock API（页面壳+错误监听） | 是（output/playwright/report/results.json） | 3 通过 | /open/api-resource（+ /open/api-resource/list） |
| open-platform-portal.spec.ts | 3 | mock API（Token→目录→审计链路） | 是 | 3 通过 | /open/portal（+ /open/oauth2/token、/open/api/v1/directory/teachers、/open/debug/run/add） |
| open-app.spec.ts | 1 | 真实后端（需 e2eRuntime.apiBaseUrl + accessToken） | **否** | 未执行 | /open/app 生命周期（add/edit/changeStatus/resetSecret/remove） |
| open-vendor-apply.spec.ts | 7 | mock auth/portal API | **否** | 用例存在，未在执行记录中 | /open/vendor-apply（公开入驻入口） |

> 仓库内存在 `han-ui/output/playwright/report/results.json` 与 `test-results/.last-run.json`（`status: passed`，`failedTests: []`），其中只包含 admin + portal 两个 spec 共 6 个用例，全部通过；`open-app.spec.ts`、`open-vendor-apply.spec.ts` 未包含在该次执行记录中。

### 3.1 厂商公开申请门户（`/open/vendor-apply`）——7 个 e2e 用例 + 现网公开页证据

`han-ui/tests/e2e/specs/open-vendor-apply.spec.ts` 共 7 个用例（mock auth/portal API）：

1. 登录页入口可进入申请页
2. 验证码启用和明确关闭状态分别可见
3. 验证码请求失败时保留校验并禁止提交
4. 注册公钥不可用时禁止提交并显示提示
5. HTTP 环境默认禁止提交密码
6. HTTP 测试兼容开启时提交明文密码且显示警示
7. 成功提交显示申请编号并清空密码，状态查询错误有提示

后端配套 JUnit（`han-auth`）：`AuthControllerVendorRegistrationTest`（1 个：`vendorPublicKeyAlwaysIncludesEnabledFlagAndKey`）+ `VendorRegistrationServiceTest`（9 个：RSA 解密、明文/HTTP 测试兼容开关、验证码、补偿账号、DTO 脱敏等）。

**现网公开页证据（生产只读）**：全流程方案 17.3 记录「用户域名 `/manage/` 及所有 JS、CSS、favicon 均返回 200；真实 Chromium 可见厂商申请页，控制台错误 0、失败请求 0」。即公开厂商申请页已有真实浏览器只读验收证据；管理端登录后四页（C01）仍无生产证据。

## 四、统计与缺口

### 4.1 统计

| 指标 | 数值 |
|---|---|
| Handler 总数（10 个 Controller 的 handler 方法） | **55** |
| 有行为级自动化覆盖（Controller 直测 / 服务层单测 / 已执行 e2e，任一命中） | **37** |
| 仅注解/反射规范级覆盖（无行为级测试，仅 OpenPlatformControllerStandardsTest 等枚举断言） | **18** |
| 完全无证据（连注解规范都未覆盖） | **0** |

> 注：「有行为级自动化覆盖」37 = 55 − 18。其中一部分是服务层单测（handler 本身未直测，但核心逻辑已测），已在表中逐行标注。

### 4.2 缺口清单（18 个仅注解规范级 handler）

1. OAuth2Controller#authorize —— 授权码签发链路无单测（未登录 401 分支也未测）。
2. OAuth2Controller#authorizeConfirm —— 拒绝授权 / 未登录分支未测。
3. OAuth2Controller#userInfo —— `getUserInfo` 服务方法无单测。
4. OpenApiResourceController#offline —— 下线行为无单测。
5. OpenApiResourceController#getDetail —— `getDetail` 服务方法无单测。
6. OpenApiResourceController#deprecateVersion —— 废弃版本行为无单测。
7. OpenAppController#list —— `selectPage` 无单测（e2e 存在未执行）。
8. OpenAppController#edit —— `update` 无单测（e2e 存在未执行）。
9. OpenAppController#remove —— `deleteById` 无单测（e2e 存在未执行）。
10. OpenAppController#resetSecret —— `resetAppSecret` 无单测（e2e 存在未执行）。
11. OpenAppController#changeStatus —— `updateStatus` 无单测（e2e 存在未执行）。
12. OpenVendorController#list —— 厂商分页无单测。
13. OpenVendorController#getDetail —— 厂商详情无单测。
14. OpenVendorController#listMyVendors —— 当前用户厂商列表无单测。
15. OpenVendorController#updateStatus —— 厂商状态变更无单测。
16. SsoController#ssoLogin —— 登录换 ticket / redirect_uri 非法分支无单测。
17. SsoController#ssoLogout —— 登出清 token 逻辑无单测。
18. SsoController#checkLogin —— 极简端点无单测。

### 4.3 其他系统性缺口（非逐 handler）

- **Controller 层直测缺口**：OpenAppAuthorizationController（8 个）、OpenAppController（多数）、OpenVendorController（多数）、OpenApiResourceController（版本类）、IOpenVendorController 主要覆盖在服务层，handler 参数绑定/返回包装未直测。
- **权限拒绝分支**：各 `@PreAuthorize` 权限点的「无权限 → 403」、`@AdminAuth`「未登录 → 401」、`@InnerAuth`「无/错误内部签名」分支均无自动化测试（仅注解存在性被断言）。
- **限流行为**：OpenDirectoryController 3 个端点的 `@RateLimiter(IP)` 未测。
- **生产证据**：公开厂商申请页已有现网只读验收（真实 Chromium，控制台/失败请求均 0，见 3.1）；其余管理端/授权/目录等接口**无**生产只读/主链路实测记录，本账不将未执行项补表为通过。
- **未执行 e2e**：`open-app.spec.ts`（真实后端，1 个用例）存在于仓库但未执行；`open-vendor-apply.spec.ts`（mock，7 个用例）用例已枚举（见 3.1），但不在最近一次 Playwright 执行记录中，待纳入执行记录。

## 五、mvn test 复跑附注（只读验证，未改代码）

命令（按任务指定）：

```text
mvn -gs settings.workspace.xml -pl han-modules/han-open -am test
```

结果（2026-08-23 12:15，Maven 3.9.12 / JDK 21）：

- **BUILD SUCCESS**（reactor 15 个模块全部 SUCCESS，含 han-common*、han-api-*、han-open）。
- han-open 模块 surefire 汇总：**Tests run: 139, Failures: 0, Errors: 0, Skipped: 0**（24 个测试类）。
- 依赖模块 han-common-core 同步执行了自身测试（ClassroomAesCodecTest、ClassroomClaimsTest 等，非开放平台相关），无失败。
- 无任何失败项；本次运行仅重跑已有测试，未新增/修改任何代码与测试。

### 附：han-open 24 个测试类逐类用例数（本次复跑）

| 测试类 | Tests |
|---|---|
| OAuth2ControllerTest | 1 |
| OpenApiResourceControllerTest | 3 |
| OpenApiTestRunControllerTest | 1 |
| OpenDirectoryControllerTest | 8 |
| OpenPlatformControllerStandardsTest | 5 |
| OpenVendorControllerStandardsTest | 2 |
| SsoControllerTest | 4 |
| OpenPlatformConverterTest | 3 |
| OpenVendorConverterTest | 2 |
| OpenGlobalDirectoryTenantExcludeTest | 1 |
| OpenPlatformPoMappingTest | 8 |
| HanOpenApplicationObjectMapperTest | 1 |
| HanOpenInnerAuthConfigTest | 1 |
| OAuth2ServiceImplTest | 12 |
| OAuth2TokenLifecycleTest | 2 |
| OpenAdminQueryServiceTest | 3 |
| OpenApiResourceServiceImplTest | 4 |
| OpenApiTestRunServiceImplTest | 8 |
| OpenAppAuthorizationEnvironmentTest | 19 |
| OpenAppAuthorizationServiceImplTest | 25 |
| OpenAppServiceImplTest | 7 |
| OpenVendorPortalServiceTest | 8 |
| OpenVendorServiceImplTest | 7 |
| ResourcePathMappingServiceImplTest | 4 |
| **合计** | **139** |
