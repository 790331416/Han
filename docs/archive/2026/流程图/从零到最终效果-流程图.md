# Han 从零到最终效果流程图

## 1. 使用说明

这份图是根据 [从零到最终效果.md](/D:/code/Han/docs/从零到最终效果.md) 进一步压成可视化流程图的版本。

阅读口径：

- 只画当前已经开发并且已经测绿的主链路
- `medium` 继承 `small`
- `full` 继承 `medium`
- AI 单独拆出细流程，避免总图过于拥挤

## 2. 全项目总流程

```mermaid
flowchart TD
    A["选择环境<br/>small / medium / full"] --> B["登录<br/>验证码 + 管理员登录<br/>已测绿"]
    B --> C["Small 核心后台<br/>已测绿"]
    C --> C1["系统管理页<br/>user role menu dept post dict config"]
    C --> C2["通知中心闭环<br/>查看 已读 全部已读"]
    C --> C3["任务调度闭环<br/>列表 日志 暂停 恢复 立即执行"]
    C --> D["Medium 增量业务<br/>已测绿"]
    D --> D1["租户管理<br/>tenant package quota"]
    D --> D2["工作流基础页<br/>definition instance todo done"]
    D --> D3["开放平台<br/>创建 编辑 启停 重置密钥 删除"]
    D --> D4["OSS 上传<br/>配置 启用 上传 URL 可访问"]
    D --> E["Full 非 AI 增量<br/>已测绿"]
    E --> E1["代码生成<br/>导入表 预览 ZIP 下载 清理"]
    E --> F["Full AI 主链路<br/>已测绿"]
    F --> G["进入 AI 细流程"]
```

## 3. AI 从零到最终效果

```mermaid
flowchart TD
    A["AI 起点"] --> B["1. 建模型<br/>ai/model<br/>已测绿"]
    B --> C{"2. 需要知识来源吗"}
    C -->|是| D["建知识库<br/>创建库 上传文档 命中测试<br/>已测绿"]
    C -->|否| E["跳过知识库"]
    D --> F{"3. 需要工具轨迹吗"}
    E --> F
    F -->|是| G["建 MCP<br/>创建服务 刷新工具<br/>已测绿"]
    F -->|否| H["跳过 MCP"]
    G --> I{"4. 是否需要 Prompt 模板"}
    H --> I
    I -->|需要| J["Prompt 模板<br/>创建 编辑 预览<br/>已测绿"]
    I -->|可选跳过| K["不阻塞主链路"]
    J --> L["5. 建智能体或工作流<br/>绑定 modelId<br/>按需绑定知识库和 MCP<br/>已测绿"]
    K --> L
    L --> M["6. 发布<br/>已测绿"]
    M --> N["7. 应用详情页<br/>概览 设置 调试 发布 访问入口 日志<br/>已测绿"]
    N --> O["8. 聊天页最终效果<br/>ai/chat<br/>已测绿"]
    O --> P{"最终看到什么"}
    P -->|只绑模型| Q["普通 AI 对话"]
    P -->|模型 + 知识库| R["knowledgeSources"]
    P -->|模型 + MCP| S["toolExecutions"]
    P -->|模型 + 知识库 + MCP| T["完整结构化效果<br/>来源洞察 + 工具轨迹"]
```

## 4. 按目标选最短路径

```mermaid
flowchart TD
    A["你想先看什么"] --> B{"目标类型"}
    B -->|后台主链路| C["登录 -> small -> medium -> full 代码生成"]
    B -->|最快 AI 回复| D["模型 -> 智能体或工作流 -> 发布 -> 聊天"]
    B -->|知识来源| E["模型 -> 知识库 -> 工作流 -> 发布 -> knowledgeSources"]
    B -->|工具轨迹| F["模型 -> MCP -> 工作流 -> 发布 -> toolExecutions"]
    B -->|最完整 AI 效果| G["模型 -> 知识库 -> MCP -> 工作流 -> 发布 -> 详情页 -> 聊天"]
```

## 5. 当前边界

```mermaid
flowchart LR
    A["已开发并测绿"] --> B["small 主链路"]
    A --> C["medium 主链路"]
    A --> D["full 主链路"]
    A --> E["full AI 主链路"]
    F["当前边界"] --> G["AI Graph<br/>未开发 -> 404"]
    F --> H["Embed Chat<br/>未开发 -> 404"]
    F --> I["pdf / docx<br/>允许上传 但不自动解析"]
```

## 6. 当前测试口径

- 页面级健康回归：
  - `small 16 passed`
  - `medium 25 passed`
  - `full 35 passed`
- Full AI 专项：
  - `25 passed`
- 这份图中的“已测绿”指的是当前真环境已验证通过，不是只靠静态路由判断
