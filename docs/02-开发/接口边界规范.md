# 接口边界规范

## 原则

- 不改变已有业务语义时，不随意变更响应结构
- 页面级可见不等于功能级可用，必须同时关注接口错误与页面错误
- 对运行时能力、路由守卫和菜单降级相关接口保持一致口径

## 测试要求

- 页面回归要同时捕获 HTTP `4xx/5xx`、业务 `code != 200`、`pageerror`、`console error`
- 新增页面时应考虑是否纳入页面级健康回归
