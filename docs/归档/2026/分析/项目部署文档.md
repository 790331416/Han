# han Cloud 项目部署文档

## 📖 文档说明

本文档详细介绍 han Cloud 项目的部署方式，包括 Docker 部署、Kubernetes 部署和本地开发环境部署。

---

## 目录

- [1. 部署方案选择](#1-部署方案选择)
- [2. Docker Compose 部署](#2-docker-compose-部署)
- [3. Kubernetes 部署](#3-kubernetes-部署)
- [4. 本地开发环境部署](#4-本地开发环境部署)
- [5. 配置说明](#5-配置说明)
- [6. 运维管理](#6-运维管理)
- [7. 故障排查](#7-故障排查)

---

## 1. 部署方案选择

### 1.1 部署规模对比

根据您的使用场景，选择合适的部署规模：

| 规模 | 适用场景 | 硬件要求 | 中间件组合 | 部署文件 |
|------|----------|----------|-----------|----------|
| **小型（极简版）** | 个人开发/学习/演示 | 2核4GB | PostgreSQL + Redis + Nacos + RustFS | `docker-compose-small.yml` |
| **中型（标准版）** | 小团队/测试环境 | 4核8GB | + RabbitMQ | `docker-compose.yml` |
| **大型（企业版）** | 生产环境/大规模 | 8核16GB+ | + Kafka + Elasticsearch | Kubernetes 部署 |

### 1.2 组件依赖关系

```
核心组件 (必需)
├── PostgreSQL  - 主数据库
├── Redis       - 缓存 + 分布式锁
├── Nacos       - 服务注册 + 配置中心
└── RustFS      - 高性能对象存储 (S3 兼容)

标准组件 (中型部署)
└── RabbitMQ    - 异步消息队列

企业组件 (大型部署)
├── Kafka       - 大数据流处理
└── Elasticsearch - 日志聚合 + 搜索
```

---

## 2. Docker Compose 部署

### 2.1 小型部署（推荐入门）

**适合**: 个人学习、功能演示、快速验证

#### 步骤 1：准备环境

```bash
# 安装 Docker 和 Docker Compose
# Windows: 安装 Docker Desktop
# Linux: 
sudo apt update
sudo apt install docker.io docker-compose
```

#### 步骤 2：克隆项目

```bash
git clone <repository-url>
cd han
```

#### 步骤 3：启动小型环境

```bash
# 启动所有服务
docker-compose -f docker-compose-small.yml up -d

# 查看服务状态
docker-compose -f docker-compose-small.yml ps

# 查看日志
docker-compose -f docker-compose-small.yml logs -f
```

#### 步骤 4：访问服务

等待 2-3 分钟服务完全启动后访问：

- **Nacos**: http://localhost:8848/nacos (han/han@2026)
- **网关**: http://localhost:8080
- **前端**: http://localhost:80

#### 包含服务

- ✅ PostgreSQL (数据库)
- ✅ Redis (缓存 + 分布式锁)
- ✅ Nacos (服务注册 + 配置中心)
- ✅ RustFS (对象存储)
- ✅ Gateway (网关)
- ✅ Auth (认证服务)
- ✅ System (系统管理)
- ✅ Job (JobFlow 任务调度)

#### 停止服务

```bash
# 停止所有服务
docker-compose -f docker-compose-small.yml down

# 停止并删除数据卷
docker-compose -f docker-compose-small.yml down -v
```

### 2.2 中型部署（推荐团队）

**适合**: 小团队开发、测试环境、完整功能验证

#### 步骤 1：启动中型环境

```bash
# 启动所有服务
docker-compose up -d

# 查看服务状态
docker-compose ps
```

#### 额外包含服务

- ✅ RabbitMQ (异步消息队列)
- ✅ Tenant (租户管理)
- ✅ Workflow (工作流)
- ✅ Open (开放平台)
- ✅ Gen (代码生成)
- ✅ File (文件服务)

#### 访问额外服务

- **RabbitMQ**: http://localhost:15672 (guest/guest)
- **RustFS Console**: http://localhost:9001 (hanadmin/han@2026)

### 2.3 Docker Compose 配置示例

#### docker-compose-small.yml 示例

```yaml
version: '3.8'

services:
  # PostgreSQL 数据库
  postgres:
    image: postgres:18.1
    container_name: han-postgres
    environment:
      POSTGRES_DB: han
      POSTGRES_USER: han
      POSTGRES_PASSWORD: han@2026
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./sql/archive/postgres-legacy/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - han-network

  # Redis 缓存
  redis:
    image: redis:7-alpine
    container_name: han-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - han-network

  # Nacos 服务注册与配置中心
  nacos:
    image: nacos/nacos-server:v3.1.0
    container_name: han-nacos
    environment:
      MODE: standalone
      SPRING_DATASOURCE_PLATFORM: postgres
      NACOS_AUTH_ENABLE: true
    ports:
      - "8848:8848"
      - "9848:9848"
    depends_on:
      - postgres
    volumes:
      - nacos_data:/home/nacos/data
    networks:
      - han-network

  # RustFS 对象存储
  rustfs:
    image: rustfs/rustfs:latest
    container_name: han-rustfs
    environment:
      RUSTFS_ROOT_USER: hanadmin
      RUSTFS_ROOT_PASSWORD: han@2026
    ports:
      - "9000:9000"  # API 端口
      - "9001:9001"  # Console 端口
    volumes:
      - rustfs_data:/data
    command: server /data --console-address ":9001"
    networks:
      - han-network

  # 网关服务
  gateway:
    image: han/gateway:latest
    container_name: han-gateway
    environment:
      SPRING_PROFILES_ACTIVE: docker
      NACOS_SERVER_ADDR: nacos:8848
    ports:
      - "8080:8080"
    depends_on:
      - nacos
      - redis
    networks:
      - han-network

  # 认证服务
  auth:
    image: han/auth:latest
    container_name: han-auth
    environment:
      SPRING_PROFILES_ACTIVE: docker
      NACOS_SERVER_ADDR: nacos:8848
    ports:
      - "9200:9200"
    depends_on:
      - nacos
      - postgres
      - redis
    networks:
      - han-network

  # 系统服务
  system:
    image: han/system:latest
    container_name: han-system
    environment:
      SPRING_PROFILES_ACTIVE: docker
      NACOS_SERVER_ADDR: nacos:8848
    ports:
      - "9201:9201"
    depends_on:
      - nacos
      - postgres
      - redis
    networks:
      - han-network

  # 任务调度服务
  job:
    image: han/job:latest
    container_name: han-job
    environment:
      SPRING_PROFILES_ACTIVE: docker
      NACOS_SERVER_ADDR: nacos:8848
    ports:
      - "9204:9204"
    depends_on:
      - nacos
      - postgres
      - redis
    networks:
      - han-network

volumes:
  postgres_data:
  redis_data:
  nacos_data:
  rustfs_data:

networks:
  han-network:
    driver: bridge
```

---

## 3. Kubernetes 部署

### 3.1 大型部署（生产环境）

**适合**: 生产环境、高可用、大规模用户

#### 前置条件

- Kubernetes 集群 (v1.25+)
- kubectl 命令行工具
- Helm 3.0+

#### 步骤 1：准备 Kubernetes 配置

```bash
# 创建命名空间
kubectl create namespace han-cloud

# 创建配置 ConfigMap
kubectl apply -f k8s/configmap.yaml -n han-cloud

# 创建密钥 Secret
kubectl apply -f k8s/secret.yaml -n han-cloud
```

#### 步骤 2：部署中间件

```bash
# 部署 PostgreSQL
helm install postgres bitnami/postgresql -n han-cloud \
  --set auth.username=han \
  --set auth.password=han@2026 \
  --set auth.database=han

# 部署 Redis
helm install redis bitnami/redis -n han-cloud \
  --set auth.enabled=false

# 部署 Nacos
helm install nacos nacos/nacos -n han-cloud \
  --set mode=cluster \
  --set replicaCount=3

# 部署 RustFS （对象存储）
helm install rustfs rustfs/rustfs -n han-cloud \
  --set auth.rootUser=hanadmin \
  --set auth.rootPassword=han@2026 \
  --set persistence.size=100Gi
```

#### 步骤 3：部署业务服务

```bash
# 部署网关
kubectl apply -f k8s/gateway-deployment.yaml -n han-cloud

# 部署认证服务
kubectl apply -f k8s/auth-deployment.yaml -n han-cloud

# 部署系统服务
kubectl apply -f k8s/system-deployment.yaml -n han-cloud

# 部署其他业务模块
kubectl apply -f k8s/tenant-deployment.yaml -n han-cloud
kubectl apply -f k8s/workflow-deployment.yaml -n han-cloud
kubectl apply -f k8s/job-deployment.yaml -n han-cloud
```

#### 步骤 4：配置 Ingress

```bash
# 部署 Ingress
kubectl apply -f k8s/ingress.yaml -n han-cloud
```

#### Deployment 配置示例

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: han-gateway
  namespace: han-cloud
spec:
  replicas: 3
  selector:
    matchLabels:
      app: han-gateway
  template:
    metadata:
      labels:
        app: han-gateway
    spec:
      containers:
      - name: gateway
        image: han/gateway:latest
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "k8s"
        - name: NACOS_SERVER_ADDR
          value: "nacos-service:8848"
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: han-gateway-service
  namespace: han-cloud
spec:
  selector:
    app: han-gateway
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP
```

### 3.2 Kubernetes 运维命令

```bash
# 查看所有 Pod
kubectl get pods -n han-cloud

# 查看服务状态
kubectl get svc -n han-cloud

# 查看日志
kubectl logs -f <pod-name> -n han-cloud

# 扩容/缩容
kubectl scale deployment han-gateway --replicas=5 -n han-cloud

# 更新镜像
kubectl set image deployment/han-gateway gateway=han/gateway:v2.0 -n han-cloud

# 重启服务
kubectl rollout restart deployment/han-gateway -n han-cloud

# 查看部署历史
kubectl rollout history deployment/han-gateway -n han-cloud

# 回滚
kubectl rollout undo deployment/han-gateway -n han-cloud
```

---

## 4. 本地开发环境部署

### 4.1 启动中间件（仅中间件）

```bash
# 创建 docker-compose-dev.yml，仅包含中间件
docker-compose -f docker-compose-dev.yml up -d
```

#### docker-compose-dev.yml 示例

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:18.1
    container_name: han-postgres-dev
    environment:
      POSTGRES_DB: han
      POSTGRES_USER: han
      POSTGRES_PASSWORD: han@2026
    ports:
      - "5432:5432"
    volumes:
      - ./sql/archive/postgres-legacy/init.sql:/docker-entrypoint-initdb.d/init.sql

  redis:
    image: redis:7-alpine
    container_name: han-redis-dev
    ports:
      - "6379:6379"

  nacos:
    image: nacos/nacos-server:v3.1.0
    container_name: han-nacos-dev
    environment:
      MODE: standalone
    ports:
      - "8848:8848"
```

### 4.2 导入项目到 IDEA

1. 打开 IntelliJ IDEA
2. File → Open → 选择项目根目录
3. 等待 Maven 依赖下载完成

### 4.3 配置 Nacos

1. 访问 Nacos 控制台: http://localhost:8848/nacos (nacos/nacos)
2. 创建命名空间: `han-cloud`
3. 导入配置文件: `sql/shared/nacos/templates/jobflow-scheduler.yml`

### 4.4 启动业务服务

在 IDEA 中按顺序启动：

1. `hanGatewayApplication` (网关)
2. `hanAuthApplication` (认证服务)
3. `hanSystemApplication` (系统服务)
4. 其他业务模块...

### 4.5 启动前端

```bash
cd han-ui
npm install
npm run dev
```

访问: http://localhost:80

---

## 5. 配置说明

### 5.1 环境变量

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| `SPRING_PROFILES_ACTIVE` | 激活的配置文件 | `dev` |
| `NACOS_SERVER_ADDR` | Nacos 地址 | `localhost:8848` |
| `DB_HOST` | 数据库主机 | `localhost` |
| `DB_PORT` | 数据库端口 | `5432` |
| `DB_NAME` | 数据库名称 | `han` |
| `DB_USER` | 数据库用户 | `han` |
| `DB_PASSWORD` | 数据库密码 | `han@2026` |
| `REDIS_HOST` | Redis 主机 | `localhost` |
| `REDIS_PORT` | Redis 端口 | `6379` |

### 5.2 Nacos 配置

#### jobflow-scheduler.yml

```yaml
spring:
  jobflow:
    # JobFlow 自研任务调度框架配置
    scheduler-name: hanScheduler
    cluster-mode: true
    thread-pool-size: 10
    persistence:
      type: jdbc
      driver-delegate: org.postgresql.Driver
```

### 5.3 数据库初始化

执行顺序：
1. `sql/archive/legacy-root/han_system.sql` - 系统表
2. `sql/archive/legacy-root/han_data.sql` - 初始数据
3. `sql/archive/legacy-root/han_job.sql` - 任务表
4. `sql/archive/legacy-root/han_workflow.sql` - 工作流表
5. `sql/archive/legacy-root/han_open.sql` - 开放平台表

---

## 6. 运维管理

### 6.1 健康检查

```bash
# 检查服务健康状态
curl http://localhost:8080/actuator/health

# 检查详细信息
curl http://localhost:8080/actuator/info
```

### 6.2 日志管理

#### Docker Compose 日志

```bash
# 查看所有服务日志
docker-compose logs -f

# 查看特定服务日志
docker-compose logs -f gateway

# 限制日志行数
docker-compose logs --tail=100 gateway
```

#### Kubernetes 日志

```bash
# 查看 Pod 日志
kubectl logs -f <pod-name> -n han-cloud

# 查看前 N 行日志
kubectl logs --tail=100 <pod-name> -n han-cloud

# 查看多个副本日志
kubectl logs -l app=han-gateway -n han-cloud
```

### 6.3 数据备份

#### PostgreSQL 备份

```bash
# 备份数据库
docker exec han-postgres pg_dump -U han han > backup_$(date +%Y%m%d).sql

# 恢复数据库
docker exec -i han-postgres psql -U han han < backup_20260129.sql
```

#### Redis 备份

```bash
# 触发 RDB 快照
docker exec han-redis redis-cli BGSAVE

# 复制备份文件
docker cp han-redis:/data/dump.rdb ./redis_backup_$(date +%Y%m%d).rdb
```

### 6.4 监控告警

#### Prometheus + Grafana (可选)

```bash
# 部署 Prometheus
docker-compose -f docker-compose-monitoring.yml up -d prometheus

# 部署 Grafana
docker-compose -f docker-compose-monitoring.yml up -d grafana
```

访问:
- Prometheus: http://localhost:9090
- Grafana: http://localhost:3000 (admin/admin)

---

## 7. 故障排查

### 7.1 常见问题

#### 问题 1: 服务启动失败

**症状**: 容器启动后立即退出

**排查步骤**:
```bash
# 查看容器日志
docker logs <container-name>

# 查看容器状态
docker inspect <container-name>

# 检查环境变量
docker exec <container-name> env
```

**常见原因**:
- 数据库连接失败
- Nacos 配置未加载
- 端口被占用

#### 问题 2: 服务无法注册到 Nacos

**排查步骤**:
1. 检查 Nacos 是否正常运行
2. 检查网络连通性: `ping nacos`
3. 查看 Nacos 日志
4. 检查服务配置中的 Nacos 地址

#### 问题 3: 数据库连接失败

**排查步骤**:
```bash
# 测试数据库连接
docker exec -it han-postgres psql -U han -d han

# 检查数据库日志
docker logs han-postgres

# 检查数据库配置
docker exec han-postgres cat /var/lib/postgresql/data/postgresql.conf
```

#### 问题 4: Redis 连接失败

**排查步骤**:
```bash
# 测试 Redis 连接
docker exec -it han-redis redis-cli ping

# 检查 Redis 日志
docker logs han-redis
```

### 7.2 性能优化

#### JVM 参数调优

```bash
# 在 Dockerfile 或 docker-compose.yml 中设置
JAVA_OPTS=-Xms512m -Xmx1024m -XX:+UseG1GC -XX:MaxGCPauseMillis=200
```

#### 数据库连接池

```yaml
spring:
  datasource:
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
```

### 7.3 应急处理

#### 服务重启

```bash
# Docker Compose
docker-compose restart <service-name>

# Kubernetes
kubectl rollout restart deployment/<deployment-name> -n han-cloud
```

#### 紧急回滚

```bash
# Kubernetes 回滚到上一个版本
kubectl rollout undo deployment/<deployment-name> -n han-cloud

# 回滚到指定版本
kubectl rollout undo deployment/<deployment-name> --to-revision=2 -n han-cloud
```

---

## 附录

### A. 快速部署脚本

#### deploy-small.sh (Linux/Mac)

```bash
#!/bin/bash
echo "🚀 正在启动 han Cloud (小型部署)..."
docker-compose -f docker-compose-small.yml up -d
echo "⏳ 等待服务启动..."
sleep 30
echo "✅ 部署完成！"
echo "📝 Nacos: http://localhost:8848/nacos (han/han@2026)"
echo "💾 RustFS: http://localhost:9001 (hanadmin/han@2026)"
echo "🌐 网关: http://localhost:8080"
```

#### deploy-small.bat (Windows)

```batch
@echo off
echo 🚀 正在启动 han Cloud (小型部署)...
docker-compose -f docker-compose-small.yml up -d
echo ⏳ 等待服务启动...
timeout /t 30
echo ✅ 部署完成！
echo 📝 Nacos: http://localhost:8848/nacos (han/han@2026)
echo 💾 RustFS: http://localhost:9001 (hanadmin/han@2026)
echo 🌐 网关: http://localhost:8080
pause
```

### B. 端口映射表

| 服务 | 容器内端口 | 宿主机端口 | 用途 |
|------|----------|----------|------|
| Gateway | 8080 | 8080 | 网关 API |
| Auth | 9200 | 9200 | 认证服务 |
| System | 9201 | 9201 | 系统管理 |
| Tenant | 9202 | 9202 | 租户管理 |
| Workflow | 9203 | 9203 | 工作流 |
| Job | 9204 | 9204 | 任务调度 |
| Open | 9205 | 9205 | 开放平台 |
| Gen | 9206 | 9206 | 代码生成 |
| File | 9207 | 9207 | 文件服务 |
| PostgreSQL | 5432 | 5432 | 数据库 |
| Redis | 6379 | 6379 | 缓存 |
| Nacos | 8848 | 8848 | 注册/配置 |
| RustFS | 9000, 9001 | 9000, 9001 | 对象存储 (API/Console) |
| RabbitMQ | 5672, 15672 | 5672, 15672 | 消息队列 |

---

**© han Cloud Team - 项目部署文档**
