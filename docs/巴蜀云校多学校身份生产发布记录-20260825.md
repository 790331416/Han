# 巴蜀云校多学校身份生产发布记录（2026-08-25）

## 1. 发布范围

- 功能源码：`02916e6`（一账号多学校身份、身份会话、课堂凭证、数字校园、共享账号生命周期）。
- 发布文档：`769b3d7`、`9e65aa3`。
- 本次发布后端：`han-system`、`han-auth`、`han-gateway`。
- 本次发布前端：管理端 `/manage/` 静态包。
- 校端根站 `/` 与 H5 `/h5/` 未换包，仅完成正式入口回归。

## 2. 正式访问入口

| 端 | 正式入口 | 本次结果 |
| --- | --- | --- |
| 管理端 | `http://www.lunengbashushuzhixuexiao.com/manage/login?redirect=/` | 200；新构建 JS 返回 `application/javascript` |
| 校端 | `http://www.lunengbashushuzhixuexiao.com/` | 200；未换包 |
| H5 | `http://www.lunengbashushuzhixuexiao.com/h5/` | 200；未换包 |

旧 `219.151.191.186:48280` 不再作为用户直连地址。管理端仍由统一域名 `/manage/` 访问，内部源站仅用于发布和回滚。

## 3. 数据库与会话处理

- DBA 已执行 `idx_edu_person_identity`，回读列顺序为 `tenant_id,user_id,status,del_flag`。
- 变更仅新增 `edu_person` 二级索引，不修改业务数据。
- 切换前完成 MySQL 一致性逻辑备份、Redis RDB 备份、现网 JAR/静态包/编排配置备份。
- 应用数据库账号无读取 Event Scheduler 定义权限；备份已包含可读取的表结构、数据、触发器和 routines。
- 为使新身份会话索引从干净状态开始，已受控清理旧 Han Access Token、Refresh Token、登录索引、在线索引和三课堂临时凭证；所有用户需重新登录。

## 4. 制品核验

| 制品 | SHA-256 |
| --- | --- |
| `han-gateway.jar` | `16e6f7d4a3fc2e2895012ee85dfd01e6ea776960f7727a3fa785dea66d92fea5` |
| `han-auth.jar` | `c5b5dd358c623a1bfcfe4864082389f72b26b06ff906d76ea246e67fcf482aca` |
| `han-system.jar` | `37150f1a920fdaba99efc55b4e4f9e75d4609e0b29c9d82081cb03d772f40257` |
| 管理端发布包 | `7907ea497c98fe26657edc5a4bec857c8d0c1c658f25b5aaa2d9d766c2d84fd0` |

远端 staging、运行目录与本地制品哈希一致后才执行切换。

## 5. 技术回读

- `han-system`、`han-auth`、`han-gateway` 完成重建并保持运行。
- 网关健康检查：200。
- 公开品牌接口：200。
- 未携带 Token 的用户信息接口：401。
- 验证码接口：200。
- 管理端 Nginx 配置语法检查通过。
- 后端完整 `clean test`：`han-auth` 80 项、`han-system` 345 项，均为 0 失败、0 错误。
- 管理端生产构建通过。

## 6. 回滚边界

1. 后端异常：恢复本次受控备份中的 `han-system`、`han-auth`、`han-gateway` JAR 后，分服务重建并回读健康与 401 边界。
2. 管理端异常：将本次切换前的 `web` 发布目录恢复为当前目录后重建管理端 Nginx 容器，并从正式 `/manage/` 回读静态资源。
3. 索引异常：仅在确认索引导致问题时执行 `ALTER TABLE edu_person DROP INDEX idx_edu_person_identity;`。
4. Redis RDB 仅作为受控维护窗口中的灾难恢复依据；不得在线直接全量恢复，以免覆盖发布后的新会话和业务缓存。

## 7. 仍需人工业务验收

生产当前没有已存在的多学校身份账号，因此未在生产构造测试人员或测试账号。需由具备真实业务数据的管理员完成：

1. 创建或确认同一账号在 A 校为管理员、在 B 校为教师。
2. 管理端登录选择 A 身份，确认仅能看到 A 校菜单和数据。
3. 切换或进入 B 教师身份，确认不继承 A 校管理员菜单和数据。
4. 解绑、停用、角色清空或学校停用后，确认旧会话被强制失效。
5. 校端与 H5 分别换取课堂凭证，确认身份和学校不能交叉使用。

本记录只确认技术发布与公开/未登录边界，不替代上述真实账号业务验收。

## 8. 身份登录补丁（2026-08-25 11:50）

- 源码提交：管理端与网关 `0e3f42d`；H5 `e3c99b2`；校端 `8ed17e4`。
- 修复 `POST /auth/identity/select` 未通过网关的缺口：只放行该精确 POST 路径，业务层继续原子校验并一次性消费身份票据；相邻路径和 GET 仍要求 Han Token。
- 管理端人员表单只保留“启用校端登录”：同租户同手机号的现有账号由服务端自动绑定，前端只展示脱敏提示；未修改 Excel 导入字段与既有兼容逻辑。
- 校端与 H5 均改为“登录返回多身份票据 → 选择身份 → 保存 Han Access Token → 拉取身份/换课堂凭证”，不再在无 Token 时请求身份列表。
- 校端发布包由隔离工作树构建，未包含原校端工作区中 `.env.bashu`、路由、结对页的三处未提交改动。

| 制品 | SHA-256 |
| --- | --- |
| 网关 `han-gateway.jar` | `56a3f51dde7f7b6d440df04c2ca66ae5a4ef679578458f9db74b3f5bd9332144` |
| 管理端发布包 | `8ab1ac0277671416fb42be13f648804bde1e92b43f38693584c7e6c3c3ac15ae` |
| H5 发布包 | `cfcb921377e39a9aadc1b2d3ec3cd94946de69b8a6fae954ce67e82a171ac106` |
| 校端发布包 | `f778340762c1db4b118a49211c65034179a2b3e54fd0d2111d517aca5c5a1ed7` |

本次切换前已分别备份网关 JAR、管理端 `web`、校端 `sgktUi` 与 H5 `h5`；回退时恢复对应备份目录/包并仅重建对应容器即可。

## 9. 身份选择与学校结对补丁（2026-08-25 12:44）

- 源码提交：Han 身份岗位名称 `f018950`；H5 `862d548`；校端身份弹框 `a9dc68d`；校端结对列表前端 `2abade8`；校端 API `e5ac7232`。
- 校端身份选择由全屏背景卡片改为可搜索弹框，按姓名、学校、岗位检索；顶部与控制台顶部在账号有多个身份时显示“切换身份”。
- 身份卡片展示真实岗位名称：管理员 / 普通教师，不再把“教师”这一人员类型误当岗位展示。
- H5 与校端切换身份时先调用 `/auth/identity/switch` 换发 Han Token，再换取对应 Classroom Token，禁止拿旧学校 Token 请求新学校身份。
- 学校结对新增、去重、列表统一按 `organ_id`；保留无 `organ_id` 时的名称查询兼容。校端 API 已用不存在学校 ID 回读，返回 HTTP 200 空分页。
- 生产聚合核验：2 个学生身份均已绑定正常账号，人员、学校和账号均有效；生产课堂允许角色包含 `2,4`，后端学生课堂 Token 用例通过。未使用真实学生密码自动登录，仍需业务方完成页面验收。

| 制品 | SHA-256 |
| --- | --- |
| `han-auth.jar` | `114893c30523f1f37228bdf6489c6795274a857fbfa896b810748294ee701e1f` |
| `han-system.jar` | `e00bec04811fcfdbac7bab64caa748e1432d32e8daade337bf207ed03aa0d6fa` |
| `three-classroom-api.jar` | `51637d35934f160ccb178409ee68b78d82624f8edd61e4fae6ee9e9cb3cc9347` |
| H5 发布包 | `0a58e0a3ea022b26111563da9fc89f8e559ef2d1ef7b64eba644275b5ac5bdf7` |
| 校端发布包 | `1614b3c9285782e573000b9bbbf6620296fbf21c8f3da0675d2256a7dd651d26` |

回滚目录：`/data/hanbashu/backups/identity-ui-f018950-20260825T1240`、`/data/sgkt-bashu/backups/identity-ui-e5ac7232-20260825T1242`、`/data/sgkt-bashu-web/backups/identity-ui-20260825T1244`。

## 10. HTTPS 证书挂载（2026-08-25 14:22）

- 公网入口 `113.249.107.199` 已监听 443；HTTP 80 自动跳转到 HTTPS。
- 证书覆盖 `lunengbashushuzhixuexiao.com` 和 `*.lunengbashushuzhixuexiao.com`，有效期为 2026-08-25 至 2027-03-12；证书链与私钥公钥匹配。
- `https://www.lunengbashushuzhixuexiao.com/`、`/h5/`、`/manage/` 已通过公网 TLS 与页面回读。
- Nginx 配置与 Compose 均已备份在 `/data/sgkt-bashu-web/backups/https-20260825T1422`；证书仅以受限权限挂载到容器，未写入仓库或发布记录。
- 裸域当前无 DNS A 记录，仅 `www` 指向公网入口；待 DNS 增加 `lunengbashushuzhixuexiao.com -> 113.249.107.199` 后，裸域 HTTPS 才能从公网访问。服务器侧已用 SNI 验证裸域证书配置正常。
